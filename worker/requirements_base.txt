# important: arq requires redis <6.0.0
arq>=0.28.0
redis>=5.3.1,<6.0.0

httpx>=0.28.0

# ClamAV client — talks to the clamd daemon (clamav service) over TCP
clamd>=1.0.2

# Image-variant rendering (worker-only; the sole imgtools consumer)
imgtools_m8>=2.1.0

# Pillow: used ONLY for the pre-decode image header probe (decompression-bomb
# guard, worker/image_guard.py) — a header read, never a render. All real image
# computation stays delegated to imgtools_m8. Declared explicitly because the
# worker imports PIL directly; the pin tracks imgtools_m8's own Pillow floor.
Pillow>=12.2.0

# Shared media SDK: object-storage client + producer↔consumer job contracts
# Track the latest SDK so the worker stays aligned with the shared contract set.
media-sdk-m8>=0.4.0

minio>=7.2.18

pydantic>=2.10.6
pydantic_settings>=2.7.1
